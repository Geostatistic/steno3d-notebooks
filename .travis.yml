language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5

sudo: false

addons:
  apt:
    packages:
      - python-numpy
      - python-scipy

env:
    - AUTOCLONE="y"


install:
  - pip install nose-cov
  - pip install -r requirements.txt

script:
  - nosetests

after_success:
  # - Build mybinder repo

notifications:
 slack: 3point:n4uGopqXrpbYFbslpV5z0tyO
 email: false
